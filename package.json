{
  "name": "teacheraiclub-site",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Teacher AI Club — static site built from a live Airtable directory, deployed on Cloudflare Pages.",
  "scripts": {
    "build": "node build.mjs"
  },
  "engines": {
    "node": ">=18"
  }
}
